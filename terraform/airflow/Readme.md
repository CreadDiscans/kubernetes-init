Gitlab > admin > applications 에서 airflow 생성

scopes : openid email api

Callback URL : https://airflow.${domain}/oauth-authorized/gitlab

client_id, client_secret를 tfvars 파일에 기입

Group 이름을 consoleAdmin으로 생성
